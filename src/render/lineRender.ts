import { IGLContext } from "../utils/shader";

export interface ILineRender {

}

export function createLineRender(ctx: IGLContext): ILineRender {

    // create shaders, buffers, etc

    return {

    };
}
